/* // Primero
console.log(Number.EPSILON) //diferencia entre 1 y el número mas pequeño
 */

/* // Declaracion de número
const obj = new Number(10); // Es un objeto numero; se crea numero con una funcion. Retorna array
console.log(obj);
console.log(typeof obj);
console.log(obj.valueOf()); // Retorna el valor de tipo del dato guradado en el objeto

const nan = NaN; //Not a Number
console.log(nan)
console.log(typeof nan);

const infiPos = Infinity;
console.log(infiPos)
console.log(typeof infiPos);

const infiNeg = -Infinity;
console.log(infiNeg)
console.log(typeof infiNeg); */

// Segundo isSafeIntegar(variable) //compara el valor seguro retorna Booln asegura el límete

// isFinite //no aplica para 0/0 por el resultado indefinido; retorna Booln

